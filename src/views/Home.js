import HeroImg from "../templates/HeroImg";
import SelectedProperties from "../templates/SelectedProperties";

const Home = () => {
  return (
    <>
      <HeroImg />
      <SelectedProperties />
    </>
  );
};

export default Home;
